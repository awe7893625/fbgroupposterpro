import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  output: 'export',  // Static export, served by aiohttp
  trailingSlash: true,
  images: { unoptimized: true },
}

export default nextConfig
